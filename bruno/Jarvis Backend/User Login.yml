info:
  name: User Login
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/auth/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
